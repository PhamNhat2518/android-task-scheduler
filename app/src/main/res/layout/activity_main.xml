<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <!-- 1. Thanh công cụ (Toolbar) -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/colorPrimary"
            app:navigationIcon="@drawable/ic_menu_24"
            app:title="Lịch trình"
            app:titleCentered="true"
            app:titleTextColor="@color/white"
            app:menu="@menu/toolbar_menu"
            />
        <!-- Lưu ý: bạn cần tạo tệp menu/toolbar_menu.xml -->

    </com.google.android.material.appbar.AppBarLayout>

    <!-- 2. Danh sách (RecyclerView) -->
    <!-- Lưu ý: bạn cần tạo tệp layout/list_item_schedule_day.xml -->

    <!-- 3. Thanh điều hướng dưới cùng (Bottom Navigation) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintTop_toBottomOf="@id/appBarLayout"
        app:layout_constraintVertical_bias="0.0"
        tools:itemCount="4"
        tools:layout_editor_absoluteX="0dp"
        tools:listitem="@layout/list_item_schedule_day" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorBackgroundLightGrey"
        app:itemIconTint="@color/bottom_nav_item_color"
        app:itemTextColor="@color/bottom_nav_item_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/bottom_navigation_menu" />
    <!-- Lưu ý: bạn cần tạo 2 tệp menu và 1 color selector -->

</androidx.constraintlayout.widget.ConstraintLayout>