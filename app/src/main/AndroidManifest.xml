<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppQuanLyLichTrinh"
        tools:targetApi="31">
        <!-- Đặt Activity1 làm màn hình khởi chạy -->
        <activity
            android:name=".controller.Activity1"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Khai báo các activity khác -->
        <activity android:name=".controller.Activity2" android:exported="false"/>
        <activity android:name=".controller.Activity3" android:exported="false"/>
        <activity android:name=".controller.MainActivity" android:exported="false"/>
        <activity android:name=".controller.LoginActivity" android:exported="false"/>
        <activity android:name=".controller.RegisterActivity" android:exported="false"/>
        <activity
            android:name=".controller.SettingsActivity"
            android:exported="false" />
    </application>

</manifest>